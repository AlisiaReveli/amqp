import { ENTITY_TYPE_DISCRIMINATOR } from '../../core/read/constants';
export declare abstract class Table {
    item_id: string;
    user_id: number;
    platform: string;
    campaign_id: string;
    [ENTITY_TYPE_DISCRIMINATOR]?: string;
    traffic_source_account_id: number;
    traffic_source_unique_name: string;
    isValid(): boolean;
}
