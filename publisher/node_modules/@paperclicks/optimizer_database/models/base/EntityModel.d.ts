import { ObjectType } from '../../core/types';
import { Model } from './Model';
import { GetPerformanceArgs } from '../../core/read/request/GetPerformanceRequest';
export declare class EntityModelCreate {
    constructor(model: EntityModelCreate);
    user_id: number;
    platform: string;
    traffic_source_unique_name: string;
    traffic_source_account_id: number;
}
export declare abstract class EntityModel<TCreate extends EntityModelCreate = EntityModelCreate> extends Model<TCreate> {
    user_id: number;
    platform: string;
    campaign_id: string;
    abstract traffic_source_campaign_id: string;
    traffic_source_unique_name: string;
    traffic_source_account_id: number;
    private get type();
    static getPerformance<T extends EntityModel>(this: ObjectType<T>, requestArgs: GetPerformanceArgs): Promise<void>;
}
