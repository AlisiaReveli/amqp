import { IAmqpClient } from './IAmqpClient';
export declare class AmqpService {
    private static _instance;
    private client?;
    private constructor();
    setClient(client: IAmqpClient): void;
    static get instance(): AmqpService;
    sendToQueue<T>(queue: string, payload: T, options: {
        headers: Record<string, string>;
    }): Promise<void>;
    publish<T>(exchange: string, routingKey: string, payload: T, options: {
        headers: Record<string, string>;
    }): Promise<void>;
    close(): Promise<void>;
}
