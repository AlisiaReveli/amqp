export declare class ConfigService {
    private static _instance;
    private _insertQueue;
    private _exchange;
    private _chunkSize?;
    private constructor();
    static get instance(): ConfigService;
    set insertQueue(value: string);
    get insertQueue(): string;
    get exchange(): string;
    set exchange(value: string);
    set chunkSize(chunkSize: number | undefined);
    get chunkSize(): number | undefined;
}
