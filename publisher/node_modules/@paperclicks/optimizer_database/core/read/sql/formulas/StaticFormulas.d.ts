import * as mathjs from 'mathjs';
export declare type FormulaField = {
    name: string;
    formula: string;
};
export declare class StaticFormulas {
    private readonly _formulasMap;
    constructor(customFormulas?: FormulaField[]);
    private hasKey;
    expandNode(node: mathjs.MathNode): mathjs.MathNode;
}
