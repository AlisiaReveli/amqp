import { FormulaField } from './StaticFormulas';
export declare class Formula {
    private _alias;
    private formula;
    private node;
    constructor(_alias: string, formula: string, customFormulas?: FormulaField[]);
    private expand;
    get alias(): string;
    getSql(renameFields: Record<string, string>, aggregatedFields: Set<string>): string;
    getDependencies(): string[];
    private buildSql;
}
