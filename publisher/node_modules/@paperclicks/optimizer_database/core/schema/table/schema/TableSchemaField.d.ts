import { TableSchema } from './TableSchema';
import { CHType, InsertQueryValueItem } from '../../../write/types';
export declare class TableSchemaField {
    name: string;
    table: TableSchema;
    constructor(name: string, table: TableSchema);
    private _fieldType;
    private _transformFunction?;
    set fieldType(value: CHType);
    get fieldType(): CHType;
    setTransformFunction(fn: <T, E>(data: T) => E): void;
    transformValue(data: unknown): InsertQueryValueItem;
    withSubClassTable(table: TableSchema): TableSchemaField;
}
