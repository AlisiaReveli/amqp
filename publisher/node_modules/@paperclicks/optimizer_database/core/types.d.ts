import { ModelSchema } from './schema/model/schema/ModelSchema';
export declare type ObjectType<T> = {
    new (): T;
};
export declare type ModelJoinField<TLeft = unknown, TRight = unknown> = [left: keyof TLeft, right: keyof TRight] | [both: keyof TLeft & keyof TRight];
export declare type ModelJoin<TLeft = unknown, TRight = unknown> = {
    schema: ModelSchema;
    fields: Array<ModelJoinField<TLeft, TRight>>;
};
